# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT=agent-rag-app
GOOGLE_CLOUD_LOCATION=us-central1
# We will let the script try different model names if this one fails
MODEL_NAME=gemini-2.0-flash-001

# Path to service account key file (use relative path)
GOOGLE_APPLICATION_CREDENTIALS=credentials.json

# Chunking settings
CHUNK_SIZE=1000
CHUNK_OVERLAP=100
